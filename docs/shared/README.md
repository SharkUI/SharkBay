# Shared Design References

This directory contains static HTML design artifacts.

- `tasks-design.html` is the original task protocol design proposal. It is useful for design intent, but the current implementation has superseded some details.
- `tasks-ui-mockup.html` is a static mockup that reuses production CSS for layout reference.

The current source of truth is the implementation in `src/`, the tests in `tests/`, and the Markdown docs in `docs/`.
